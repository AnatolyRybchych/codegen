#ifndef ARRAY_$upper(${Type})_H
#define ARRAY_$upper(${Type})_H

#include <stdlib.h>

typedef struct Arr$capital(${Type}) Arr$capital(${Type});

struct Arr$capital(${Type}){}{
    size_t capacity;
    size_t count;
    ${Type} elements[];
};

#endif // ARRAY_$upper(${Type})_H
